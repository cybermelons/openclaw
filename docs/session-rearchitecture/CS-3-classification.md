# CS-3 classification (PHASE-1.md §2a)

Author: Sonnet (CS-3a). Verified against the current tree at
`/home/kiri/.openclaw/worktrees/rearch-session-store` (branch `rearch/session-store`).
CS-1 (`SessionConflictError` + `withSessionRetry`) and CS-2 (`revision` column, bumps
on every `session_nodes` write) are landed; this table classifies every write and
compare/throw site so 3a/3b/3c scope is unambiguous.

## 1. `session_nodes` write sites (§2a: CAS / Blind / Upsert)

| Site                                                                  | File:line                                                                         | Class                                                                 | Revision bump present (CS-2)?                                                                                       | Notes                                                                                                                                                                                                                                                                                                                                                                                          |
| --------------------------------------------------------------------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Main entry upsert (`writeSessionEntry`)                               | `session-accessor.sqlite-entry-store.ts:664` (insert), `:699` (entry_valid touch) | Upsert / Blind                                                        | Yes — `revision: 1` on insert, `eb("session_nodes.revision","+",1)` on conflict-update and on the entry_valid touch | The insert arm starts at 1; the conflict-update arm and the follow-up `entry_valid=1` update both bump. Reached only from inside `patchSqliteSessionEntrySnapshot`'s write transaction after the CAS assert already passed (or from unguarded upsert entry points) — the CAS guard for this write lives in the _caller_ (`assertSessionEntrySelectionUnchanged`), not in this function itself. |
| Clear-preserving-windows (`clearSqliteSessionEntryPreservingWindows`) | `session-accessor.sqlite-entry-store.ts:481` (insert), `:493` (entry_valid touch) | Upsert / Blind                                                        | Yes — same pattern as above                                                                                         | Called from `deleteSessionEntryRows` when a window survives; no CAS guard at this layer (guarded by the lifecycle-level assert in the caller chain, 3b scope).                                                                                                                                                                                                                                 |
| Ensure-node write (transcript admission)                              | `session-accessor.sqlite-transcript-state.ts:172`                                 | Blind                                                                 | Yes (per task brief; not re-verified byte-for-byte in this pass — out of primary-path scope for 3a)                 | Node existence touch on transcript writer admission; fire-and-forget from the entry-CAS point of view.                                                                                                                                                                                                                                                                                         |
| Blind owner projection touch                                          | `session-accessor.sqlite-owner-projection.ts` (`owner.ts:54` per task brief)      | Blind                                                                 | Not touched by 3a                                                                                                   | Out of scope for 3a; not re-verified line-for-line this pass.                                                                                                                                                                                                                                                                                                                                  |
| `deleteSessionEntryRows` hard delete                                  | `session-accessor.sqlite-entry-store.ts:404-407`, `:431-434`                      | Blind (DELETE, not UPDATE — no revision to bump, row ceases to exist) | N/A                                                                                                                 | Row deletion; no revision semantics apply once the row is gone.                                                                                                                                                                                                                                                                                                                                |
| `entry_valid = -1` clear touch                                        | `session-accessor.sqlite-entry-store.ts:494`                                      | Blind                                                                 | Yes                                                                                                                 | Part of the clear-preserving-windows path above.                                                                                                                                                                                                                                                                                                                                               |

## 2. Entry-CAS compare/throw sites (the ~24-site sweep, entry-CAS subset)

| Site                                                                                                | File:line                                                                                                                              | Comparator used                                                                                                                                                                                                                                                                              | Carries `row`/revision today?                                                                                                                                                                                                        | Scope                                                                                                                                                                                                                                                                             |
| --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `assertSessionEntrySelectionUnchanged`                                                              | `session-accessor.sqlite-entry-store.ts:206-220`                                                                                       | `sqliteSessionEntriesEqual` (value) + `sqliteSessionSnapshotRowsEqual` (alias-set)                                                                                                                                                                                                           | **Yes** — `SqliteSessionEntrySelectionSnapshot.selected.row.revision` already flows through `ResolvedSessionEntryRow.row` (CS-2 added `revision` to `session_nodes`, so `Selectable<...session_nodes>` carries it)                   | **3a — converted this changeset.** Primary entry-CAS path (`patchSessionEntryCore`).                                                                                                                                                                                              |
| `assertLifecycleTargetSnapshotUnchanged`                                                            | `session-accessor.sqlite-entry-store.ts:344-352`                                                                                       | `sqliteLifecycleTargetSnapshotsEqual` (value, primary + alias rows)                                                                                                                                                                                                                          | **Yes, after this changeset** — `SqliteLifecycleTargetSnapshot.primary` extended to carry `revision` (see §3 below); `resolveLifecyclePrimaryEntry` now returns it                                                                   | **3a — converted this changeset.** Primary path for `patchSessionEntryTarget`.                                                                                                                                                                                                    |
| `assertLifecycleTargetUnchanged`                                                                    | `session-accessor.sqlite-entry-store.ts:523-533` (bare `throw new Error(...changed before...)`)                                        | `sqliteLifecycleTargetMatchesExpectedEntry` → `sqliteSessionEntriesEqual`                                                                                                                                                                                                                    | No — takes a live DB read + an `expectedEntry` value, no snapshot/row plumbing                                                                                                                                                       | **3b — explicitly deferred.** Task brief calls this out as "LEAVE it for 3a unless trivial"; it's a different call shape (DB + live target + expected value, not two snapshots) and converting it would mean plumbing a revision through a third call convention. Left untouched. |
| `session-accessor.sqlite-projection.ts:160`                                                         | value-compare via `sqliteSessionEntriesEqual`                                                                                          | No row/revision in scope at that call site                                                                                                                                                                                                                                                   | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-projection.ts:215`                                                         | bare-string throw, no comparator call at that exact line (raw lifecycle removal)                                                       | —                                                                                                                                                                                                                                                                                            | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-projection.ts:297`                                                         | `sqliteSessionEntriesEqual`                                                                                                            | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-projection.ts:345`                                                         | `sqliteSessionEntriesEqual`                                                                                                            | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-projection.ts:399`                                                         | `sqliteSessionEntriesEqual`                                                                                                            | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-projection.ts:548`                                                         | bare-string throw ("deleted-agent session entries changed before purge") — batch, no single comparator call                            | —                                                                                                                                                                                                                                                                                            | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-lifecycle-state.ts:248`                                                    | `sqliteSessionStateDeleteSnapshotsEqual` (a 4th, delete-snapshot-specific comparator, not one of the 3 named in ponytail #5)           | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-lifecycle-state.ts:334`                                                    | raw-JSON `JSON.stringify` compare against `expectedRawEntryJson`, not one of the 3 comparators                                         | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-lifecycle-state.ts:722` (`assertPlannedLifecycleArtifactEntriesUnchanged`) | `sqliteSessionEntriesEqual`                                                                                                            | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-replacement-projection.ts:157`                                             | raw-JSON `JSON.stringify` compare (batch, multi-key)                                                                                   | No                                                                                                                                                                                                                                                                                           | 3b                                                                                                                                                                                                                                   |
| `session-accessor.sqlite-lifecycle.ts:402`, `:443`, `:483`                                          | `sqliteLifecycleTargetSnapshotsEqual` called directly (archive/delete lifecycle, not through `assertLifecycleTargetSnapshotUnchanged`) | Snapshot type is the pre-3a `SqliteLifecycleTargetSnapshot` without a row-carrying primary at those call sites (they use `readLifecycleTargetSnapshot` result directly, would inherit the 3a revision field but the compare call itself isn't routed through the assert helper touched here) | 3b — different call shape (returns a sentinel `DELETE_EXPECTED_ENTRY_MISMATCH`, not a throw); explicitly out of the 3a fence (task brief: "Do NOT touch projection.ts/lifecycle-state.ts/replacement-projection.ts recompare sites") |

## 3. Comparators (`session-accessor.sqlite-entry-equality.ts`) — CAS vs non-CAS

| Comparator                            | CAS use (this table's throw sites)                                                                                                  | Non-CAS use (skip-no-op, leave alone)                                                                                                                                                                              |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `sqliteSessionEntriesEqual`           | Primary path (3a, flag-on branch only after this changeset) + all 3b projection/lifecycle sites above                               | `session-accessor.sqlite-lifecycle.ts:195`, `session-accessor.sqlite-projection.ts:147` — both are "did the value actually change" skip-no-op checks, not concurrency guards. Left untouched per §3 of PHASE-1.md. |
| `sqliteSessionSnapshotRowsEqual`      | Alias-row structural guard inside `assertSessionEntrySelectionUnchanged` (kept AS-IS in 3a — key-set guard, orthogonal to revision) | none found                                                                                                                                                                                                         |
| `sqliteLifecycleTargetSnapshotsEqual` | `assertLifecycleTargetSnapshotUnchanged` (3a, flag-on branch) + the three direct call sites in `sqlite-lifecycle.ts` (3b)           | none found                                                                                                                                                                                                         |

## 4. Not-entry-CAS "changed before" occurrences (repo-wide grep, grouped by subsystem)

Full sweep: `grep -rn "changed before" --include="*.ts" src/ | grep -v test`. None of the
non-`config/sessions` hits touch `session_nodes`; each subsystem group below owns its own
CAS/mismatch object, unrelated to the session entry store. Grouped, not itemized (per
task brief — "you don't need 178 rows"):

- **`state-migrations.*` sentinels** (`state-migrations.transcript-directives.ts`,
  `.mcp-oauth.ts`, `.meeting-transcripts-files.ts`, `.source-snapshot.ts`,
  `.device-identity.ts`, `.media-persistence.ts`, `.exec-approvals.ts`,
  `.subagent-registry.ts`, `.web-push.ts`, `.apns.ts`, `.workspace-setup.ts`,
  `.restart-sentinel.ts`, `.node-host.ts`, `.transcript-directives-archives.ts`,
  `.managed-outgoing-images.ts`) — one-time legacy-store migration claim CAS
  (source-file vs. Doctor-claim races). Separate migration ledger, not `session_nodes`.
- **`exec-approvals-authorization.ts`** (8 sites) — exec approval operand CAS against the
  approvals store; unrelated table.
- **`subagent-*` / `agents/subagents/**`** (`subagent-registry-restart-recovery.ts`,
  `subagent-announce-delivery-retry.ts`, `subagent-announce-dispatch.ts`,
  `subagent-registry-restart-recovery-helpers.ts`, `subagent-control-messaging.ts`) —
  subagent registry / announce-delivery lifecycle CAS, own tables.
- **`agents/embedded-agent-runner/**` + `agents/main-session-recovery/**`** — writer-claim
  and restart-recovery admission CAS. Explicitly out of scope for this changeset (CS-4 is
  concurrently editing `main-session-recovery/*`).
- **`agents/plugin-model-catalog.ts`** (3 sites) — legacy provider catalog migration CAS.
- **`agents/bash-tools.exec-host-node.ts`** — exec policy-snapshot CAS at dispatch time.
- **`agents/harness/context-engine-turn-attempt.ts`** — context-engine transcript target
  CAS, separate from `session_nodes` entry rows.
- **`gateway/**` session-facing CAS** (`server-methods/sessions-*.ts`,
  `session-reset-service.ts`, `session-create-service.ts`, `session-recovery-service.ts`,
  `session-sharing.ts`, `session-companion-ask.ts`, `agent-turn/agent-session-persist.ts`,
  `server-methods/agent-session-prepare.ts`, `server-worker-placement-startup.ts`,
  `worker-environments/**`, `server-cron.ts`, `node-registry-private.ts`,
  `node-invoke-plugin-policy.ts`) — these are gateway-level orchestration CAS wrapping
  calls _into_ the session-accessor layer (many ultimately call `patchSessionEntryCore`/
  `patchSessionEntryTarget`, which this changeset already converts); the gateway-level
  "changed before" strings themselves guard gateway-owned state (placement, worker
  claims, node pairing, cron continuation, admission), not a second independent
  `session_nodes` compare. Not converted here — no direct `session_nodes` throw at these
  lines.
- **`config/sessions/session-accessor.sqlite-transcript-write.ts`,
  `.sqlite-transcript-store.ts`, `.transcript-turn.ts`** — `SqliteTranscriptMutationConflictError`
  family; transcript rows (`session_windows`/transcript store), explicitly out of
  session_nodes-entry scope per task brief ("Do NOT touch transcript CAS").
- **`config/sessions/session-sharing-store.ts`, `session-suggestion-store.ts`,
  `legacy-main-session-migration-operations.ts`** — sharing/suggestion satellite stores
  and one-time legacy migration claims; not `session_nodes` entry CAS.
- **`skills/workshop/**`** (`collection-byte-limits.ts`, `collection-rollback.ts`,
  `service-evaluation.ts`, `apply-transition.ts`) and **`skills/lifecycle/workspace-skill-write.ts`**
  — skill-proposal collection CAS, unrelated store.
- **`snapshot/local-repository.ts`** — SQLite snapshot artifact verification CAS
  (file-level, pre-publish), not a row compare.
- **`claws/update-apply.ts`, `claws/workspace-update.ts`** — agent-update rollback CAS.
- **`commands/**`** (`onboard-agent.ts`, `agents.commands.delete.ts`, `onboard-guided.ts`,
  `doctor-heartbeat-*.ts`) — onboarding config / agent-tombstone / HEARTBEAT.md migration
  CAS, own stores.
- **`auto-reply/reply/**`** (`restart-recovery-claim.ts`, `conversation-turn-capture.ts`,
  `commands-session-store.ts`, `agent-runner-failure-reply.ts`) — durable-turn admission
  and captured-reply CAS layered above the session accessor; no direct `session_nodes`
  throw at these lines.
- **`infra/**` misc** (`push-apns-store.ts`, `system-run-approval-binding.ts`,
  `install-package-dir.ts`, `directory-durability.ts`, `sqlite-readonly-location.ts`,
  `update-managed-service-handoff.ts`) — device pairing, file-publication, and approval
  CAS; filesystem/registry level, not `session_nodes`.
- **`wizard/setup.migration-*.ts`, `system-agent/setup-*.ts`** — onboarding/inference-owner
  migration and verification CAS.
- **`plugins/runtime/runtime-agent.ts`** — plugin-created-session finalize/rollback CAS;
  wraps the accessor, no independent `session_nodes` throw.
- **`channels/turn/direct-delivery-custody.ts`** — delivery-ownership CAS, channel layer.
- **`cli/daemon-cli/lifecycle.ts`** — gateway restart-lock CAS.
- **`cron/isolated-agent/delivery-dispatch-awareness.ts`** — cron transcript-mirror CAS.
- **`media/playback-transcode.ts`** — playback source CAS.

**Gate note:** the repo-wide zero-bare-throw gate (§4 of PHASE-1.md) is a CS-3 (all
sub-phases) exit criterion, not a 3a-only one. 3a converts the two primary
`session_nodes` entry-CAS throw sites (`SqliteSessionMutationConflictError` at
`session-accessor.sqlite-entry-store.ts:216` and `:348`) to `SessionConflictError`. The
remaining `session_nodes`-adjacent bare-string sites (3b table above) and all
not-entry-CAS sites (this section) are explicitly not converted in 3a and are not part
of the zero-throw gate until their respective phase lands.

## 5. Summary — what 3a converts

- `assertSessionEntrySelectionUnchanged` (`session-accessor.sqlite-entry-store.ts:206`) —
  converted to revision-compare (default) / value-compare (flag on), throws
  `SessionConflictError` in both modes.
- `assertLifecycleTargetSnapshotUnchanged` (`session-accessor.sqlite-entry-store.ts:344`) —
  converted to revision-compare (default) / value-compare (flag on), throws
  `SessionConflictError` in both modes. Required extending `SqliteLifecycleTargetSnapshot.primary`
  with `revision` (minimal plumbing, done — see PR diff in `session-accessor.sqlite-entry-store.ts`
  around `resolveLifecyclePrimaryEntry`/`readLifecycleTargetSnapshot`).
- `assertLifecycleTargetUnchanged` (`:523`) — **left on the old bare-string throw**, deferred to 3b
  (different call shape; task brief explicitly allows deferring if plumbing balloons).
- Everything else in this table (3b/3c) — untouched.
